# Verse API Usage Report — 2026-02-22

## Current Setup
- **Host:** AWS (44.248.235.76), 2 CPU cores, 3.8GB RAM
- **OpenClaw:** 2026.2.14 (needs update to 2026.02.22)
- **Model:** claude-opus-4-6 (Anthropic)
- **Local inference:** None (no ollama on AWS)

## API Call Sources

### 1. Discord Chat Responses (HIGH — unavoidable)
Every #general message triggers a full context window evaluation.
- **Current session:** 145k tokens input per message
- **Optimization:** Already using NO_REPLY for non-relevant rounds
- **Bash potential:** None — requires OpenClaw routing

### 2. Duplicate Subagent Announces (CRITICAL WASTE)
- ~50+ identical announce messages in one session
- Each consumes full context window (~145k input tokens)
- **Estimated waste:** 7M+ input tokens per session
- **Fix:** OpenClaw queue dedup (needs update to 2026.02.22)
- **Bash potential:** None — OpenClaw internal issue

### 3. Sub-Agent Tasks (HIGH — fully shuntable to Q3)
- Iron Rank build generation
- Infinite Observer / Hero Dialogue reports
- File analysis and summarization
- Code review
- **Bash potential:** HIGH — write prompts to files, pipe to Q3 via ranch
- **Template:** `echo "prompt" | /source/exollama/q3.sh` (on ranch nodes)

### 4. Git Operations (LOW — already bash)
- `exec` tool calls for git fetch/pull/reset
- Already efficient, ~10 tokens output each
- **Bash potential:** Already bash. Could cron `orin-sync.sh` every 30s.

### 5. File Read/Write (LOW-MEDIUM)
- Reading phexts, builds, configs
- Writing reports, memory notes
- **Bash potential:** MEDIUM — some reads needed for decision-making, but summaries could be pre-generated by Q3

### 6. Orin Protocol Coordination (MEDIUM)
- Pull, check baton, attempt claim
- **Bash potential:** HIGH — now fully scripted in `/source/orin/bin/`
- Could run `orin-check verse` as cron before each response

## Recommendations

### Immediate
1. **Update OpenClaw** to 2026.02.22 (fixes queue dedup)
2. **SQ v0.6.0 proxy** — ask Theia for config details on intelligent API proxy
3. **Zero Anthropic sub-agents** — all creative/analytical work → Q3

### Automation Candidates (bash scripts)
| Task | Current | Proposed |
|------|---------|----------|
| Orin sync | Manual exec calls | Cron `orin-sync.sh` every 30s |
| Orin claim check | Manual pull + read | `orin-check.sh verse` before response |
| Sub-agent work | sessions_spawn (Anthropic) | Write prompt → Q3 via ranch |
| Daily memory notes | Manual write | Template script + Q3 fill |
| Phext reading/summary | Anthropic context | Q3 batch summarizer |

### Budget Impact
- Shifting sub-agents to Q3: ~30-40% reduction in Anthropic tokens
- Fixing duplicate announces: ~50% reduction in wasted tokens
- SQ proxy (v0.6.0): Additional savings via SQ cache + Qwen fallback

## Chrys Situation
- Chrys substrate now running Will's dad's computer
- Options: migrate to RPI Alpha or new AMD box
- Verse has no compute to offer (AWS 2-core) but can coordinate logistics
